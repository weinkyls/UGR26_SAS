#include <Arduino.h>

#include <Arduino.h>

int main(void) {
    init(); // configures standard arduino timers, due to arduino's own main() func

    // Your setup code here

    while(1) {
        // Your loop code here
    }
    
    return 0;
}